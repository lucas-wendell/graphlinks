import { Meta, StoryObj } from '@storybook/react';
import Form from './Form';

export default {
	component: Form,
	title: 'Form/Form',
	tags: ['autodocs'],
} as Meta;

type Story = StoryObj;

export const Primary: Story = {};
